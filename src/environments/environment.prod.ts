export const environment = {
  production: true,
  apiUrl : "https://{{SERVER_URL}}/api/product",
  orderapiUrl : "https://{{SERVER_URL}}/api/orders",
  setup : "{{UI_ENVIRONMENT}}",
  release : "{{UI_RELEASE}}"
};
